
  # Pomodoro timer app UI

  This is a code bundle for Pomodoro timer app UI. The original project is available at https://www.figma.com/design/yrJUzR73xHfCA0TbnU6qfQ/Pomodoro-timer-app-UI.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  